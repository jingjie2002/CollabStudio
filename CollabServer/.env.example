# =============================================================================
# NovaCollab 服务器配置模板
# =============================================================================
# ⚠️ 警告：部署时请复制此文件为 .env 并修改敏感值
# ⚠️ 不要将实际的 .env 文件提交到版本控制
# =============================================================================

# 服务器端口（80 需要 setcap 或 root 权限）
PORT=80

# JWT 密钥（⚠️ 必须修改！使用 openssl rand -base64 32 生成）
JWT_SECRET=your_secret_key_here_must_change_in_production

# CORS 白名单（多个域名用逗号分隔）
# 📌 提示：必须包含你的前端访问地址
CORS_ORIGINS=http://localhost:5173,http://119.29.55.127,wails://wails

# 数据库文件名
DB_NAME=collab.db

# 静态文件目录（前端 dist 目录）
DIST_PATH=./dist

# =============================================================================
# 部署注意事项
# =============================================================================
# 1. 复制此文件：cp .env.example .env
# 2. 修改 JWT_SECRET 为随机字符串
# 3. 修改 CORS_ORIGINS 为你的实际域名/IP
# 4. 确保 DIST_PATH 指向正确的前端构建目录
# =============================================================================
