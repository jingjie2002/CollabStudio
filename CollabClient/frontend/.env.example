# 前端环境变量配置
# 复制为 .env 或 .env.local 并根据实际环境修改

# 后端 API 地址（留空则自动检测）
# 示例: 192.168.1.100:8080 (局域网) 或 your-server.com (公网)
VITE_APP_API_URL=

# 默认服务器 IP（Wails 构建模式下的 fallback）
# 留空或设为 localhost 则连接本机
VITE_DEFAULT_SERVER_IP=localhost
